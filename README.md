# MachineLearning

Estructura:

Dada la practica yo intentare hacer lo siguiente para predecir el precio de airbnb


Primero cargar los datos con pandas en un dataframe

çon las funciones table, summary, etc vere como estan los datos del data frame y cuales son interesantes.

Una vez limpios los datos con sklearn y las funciones que tiene como train_test_split dividiremos los datos en un conjunto de test y entrenamiento para el modelo.

Observare si mis datos de entrenaniento son buenos y si la relacion tiene sentido con mas funciones como head describe y eliminare las que no crea interesantes con drop

Luego, yo usaria RandomForestRegressor para entrenar el modelo y luego utilizar alguna funcion para evaluar el modelo con el conjunto de tipo test que hayamos seleccionado.

Habra que probar varias veces y pensar bastante antes de avanzar, con la funcion corr creo que puedo sacar relaciones numericas entre variables y con el heatmap hacer correlaciones.

De esta forma elijo bien el modelo de entrenamiento y test para la prediccion.


Como es obvio no me ha dado tiempo a nada, pienso recuperar esta practica, el martes intentare subir algo de codigo pero voy muy atrasado con eso, disculpas y para la recuperacion nos vemos
